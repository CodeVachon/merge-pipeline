Print the workflow directory that would be used, and which rule chose it

Usage: merge-pipeline config path [OPTIONS]

Options:
  -f, --config <PATH>  Directory containing workflow JSON files (same as the root --config)
  -c, --cwd <DIR>      Working directory to resolve <cwd>/.merge-pipeline against
  -h, --help           Print help
